# 《此地有古人》24小时冲刺 - 修改清单

## ✅ P0 - 边界与容错（已完成）

### 1. 历史人物重名选择页
- **后端**: `backend/app/disambiguation.py` — 新增消歧义模块，硬编码10个高频重名人物映射 + 别名合并
- **后端**: `backend/app/routers/celebrity.py` — 新增 `POST /api/v1/celebrity/check-disambiguation` 接口
- **前端**: `frontend/src/api/client.ts` — 新增 `celebrityApi.checkDisambiguation()` 方法
- **前端**: `frontend/src/pages/CelebrityEntryPage.tsx` — 选择名人后检查时期（有预设时期的弹出选择页）
- **验收**: 输入"李白"→弹出时期选择页（黄州/惠州/儋州）；输入"乔布斯"→直接进入

### 2. 名人-古迹关联校验
- **后端**: `backend/app/strong_links.py` — 新增强关联映射模块（18组名人-古迹对应）
- **后端**: `backend/app/routers/guide.py` — 新增 `POST /api/v1/guide/check-link` 接口
- **前端**: `frontend/src/data/strongLinks.ts` — 前端本地校验数据
- **前端**: `frontend/src/pages/GuideSetupPage.tsx` — 创建讲解团时实时校验，弱关联弹出警告
- **前端**: `frontend/src/api/client.ts` — 新增 `guideApi.checkLink()` 方法
- **验收**: 选"李白"+"断桥"→弹出警告；选"苏东坡"+"惠州西湖"→直接通过

### 3. AI系统级防幻觉Prompt
- **后端**: `backend/app/celebrity_engine.py` — `build_celebrity_system_prompt()` 追加史实约束指令
- **后端**: `backend/app/knowledge_base.py` — `build_system_prompt()` 追加史实约束指令
- **后端**: `backend/app/routers/guide.py` — `_build_narration_system_prompt()` 追加史实约束指令
- **文案**: "你只能基于可靠史实回答。若史书记载不一，回答'史料记载存在分歧，较为可信的说法是...'。严禁编造具体年份、对话、未记载事件。若用户问及你的死亡后事件，以'我生前的了解是...'作答。若被问到你哪年去世，回答'身后之事，非我所能知也'。"
- **验收**: 问"你哪年死的"→AI回答"身后之事，非我所能知也"

### 4. 时期选择强制化与人设区分
- **前端**: `frontend/src/data/periods.ts` — 新增时期数据模块（苏东坡3期、李白3期、杜甫3期、辛弃疾3期、王维2期、许仙1期）
- **前端**: `frontend/src/pages/CelebrityEntryPage.tsx` — 有预设时期的名人必须选择时期才能开始对话
- **前端**: `frontend/src/pages/GuideSetupPage.tsx` — 有预设时期的名人必须选择时期才能创建讲解团（按钮置灰）
- **验收**: 选苏东坡→必须选黄州/惠州/儋州时期；未选时期→按钮置灰

---

## ✅ P1 - 产品核心闭环（已完成）

### 5. 右上角双模式切换：古人对话 ↔ AI导游
- **前端**: `frontend/src/pages/CelebrityChatPage.tsx` — 完全重写
  - 顶部 Segmented Control（🎭 古人对话 / 🎤 AI导游）
  - 切换时追加系统消息（仅前端展示）
  - 古人模式：暖褐色气泡 + 姓氏首字头像
  - 导游模式：深蓝灰气泡 + 虚线边框 + 🎤图标
- **验收**: 同一聊天窗口切换，AI头像/气泡样式必不同

### 6. 游客扫码落地页（仪式感）
- **前端**: `frontend/src/pages/GuideLandingPage.tsx` — 新增落地页组件
  - 全屏展示：古迹名称 + 名人圆形头像 + 时期标签 + "跨越千年的时空奇遇" + "开始对话"按钮
- **前端**: `frontend/src/pages/GuideTourViewPage.tsx` — 集成落地页，加入后先展示落地页
- **验收**: 从扫码到聊天，必须经过落地页

### 7. 讲解团时效性
- **后端**: `backend/app/models/guide_tour.py` — 新增 `expire_at` 字段 + `is_expired` 属性
- **后端**: `backend/app/routers/guide.py` — 创建时设置 `expire_at = now + 2h`；加入时校验过期
- **后端**: `backend/app/schemas/guide.py` — `TourListItem` 新增 `created_at`、`is_expired` 字段
- **验收**: 创建团后2小时过期，扫码提示"该讲解团已过期"

### 8. 语音输入入口
- **前端**: `frontend/src/pages/CelebrityChatPage.tsx` — 输入框左侧麦克风按钮
  - 使用 `webkitSpeechRecognition` API
  - 按住变红色脉冲动画
  - 不支持时按钮隐藏
- **前端**: `frontend/src/pages/GuideTourViewPage.tsx` — 同样集成语音输入
- **验收**: Chrome/Edge中可语音转文字并发送

### 9. 导游端数据看板
- **前端**: `frontend/src/pages/GuideToursPage.tsx` — 完全重写
  - 顶部3格数据概览（总讲解团/进行中/总参与人）
  - 每个团卡片展示：名人、古迹、状态、参与人数、团码、创建时间
  - 过期团显示"已过期"黄色标签
- **验收**: 列表页一眼能看到数字

---

## ✅ P2 - UI/UX与结构优化（已完成）

### 10. 首页角色分流
- **前端**: `frontend/src/pages/HomePage.tsx` — 重写
  - 顶部双大入口按钮："我是游客"（🎭）/ "我是导游"（🎙️）
  - 游客入口突出名人对话
  - 导游入口直达导游中心
- **验收**: 游客不易误点进导游创建流程

### 11. 聊天页沉浸式增强
- **前端**: `frontend/src/pages/CelebrityChatPage.tsx`
  - 顶部名人名称右侧 📜 图标
  - 点击展开历史背景时间轴（3-5个关键事件）
  - 再次点击收起，不影响聊天区
- **验收**: 点击图标必展开，内容不遮挡最新消息

### 12. 演示专用高对比度模式
- **前端**: `frontend/src/App.tsx` — 检测 URL 参数 `?demo=1`
- **前端**: `frontend/src/index.css` — `.demo-mode` 样式类
  - 背景变为 `#f5f0e8`（宣纸色）
  - 文字变为深褐色
  - 按钮保持赭石色加深
- **验收**: 访问 `?demo=1` 后所有文字对比度满足投影演示需求

---

## P3 - 代码与交付要求

- ✅ 所有新增页面保持移动端优先，使用 `max-w-[280px]`/`max-w-[300px]`/`max-w-sm` 限制宽度
- ✅ 使用现有技术栈（React 18 + Tailwind + FastAPI），未引入新依赖
- ✅ 语音API使用浏览器原生 `webkitSpeechRecognition`，无需额外依赖
- ✅ TypeScript 编译通过，Vite 构建成功
- ✅ 后端 Python 导入验证通过

---

## 文件变更汇总

### 新增文件
| 文件路径 | 说明 |
|---|---|
| `backend/app/disambiguation.py` | P0-1 重名消歧义模块 |
| `backend/app/strong_links.py` | P0-2 名人-古迹关联校验 |
| `frontend/src/data/periods.ts` | P0-4 时期数据 |
| `frontend/src/data/strongLinks.ts` | P0-2 前端关联校验 |
| `frontend/src/pages/GuideLandingPage.tsx` | P1-6 游客落地页 |

### 修改文件
| 文件路径 | 修改内容 |
|---|---|
| `backend/app/routers/celebrity.py` | 新增消歧义接口 |
| `backend/app/routers/guide.py` | 新增关联校验接口 + 过期逻辑 |
| `backend/app/celebrity_engine.py` | 防幻觉Prompt |
| `backend/app/knowledge_base.py` | 防幻觉Prompt |
| `backend/app/models/guide_tour.py` | expire_at字段 |
| `backend/app/schemas/guide.py` | 新增字段 |
| `frontend/src/App.tsx` | 路由更新 + 演示模式 |
| `frontend/src/api/client.ts` | 新增API方法 |
| `frontend/src/index.css` | 演示模式样式 |
| `frontend/src/pages/HomePage.tsx` | 角色分流 |
| `frontend/src/pages/CelebrityEntryPage.tsx` | 时期选择 |
| `frontend/src/pages/CelebrityChatPage.tsx` | 双模式+语音+时间轴 |
| `frontend/src/pages/GuideSetupPage.tsx` | 关联校验+时期强制 |
| `frontend/src/pages/GuideTourViewPage.tsx` | 落地页+语音 |
| `frontend/src/pages/GuideToursPage.tsx` | 数据看板 |
